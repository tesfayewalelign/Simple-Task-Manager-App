

import WelcomeDashboard from "./WelcomeDashboard";

export default function DashboardPage() {
  return (
    <main className="w-full min-h-screen bg-[#fcfcfc]">
      <WelcomeDashboard />
      {/* You can add other sections like statistics, tasks, etc. here */}
    </main>
  );
}
